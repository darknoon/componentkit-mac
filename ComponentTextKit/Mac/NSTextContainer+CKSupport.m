
#import "NSTextContainer+CKSupport.h"

//@implementation NSTextContainer (CKPlatform)
//
// Previously, we implemented this to return containerSize, but that's actually unnecessary since -size is already supported on 10.10 and returns the same thing (verified via disasm)
//
//@end
